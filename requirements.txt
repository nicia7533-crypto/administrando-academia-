# Corpo Dinamico - dependencias completas
# Banco de dados (sqlite3) e autenticacao (hashlib) usam so a biblioteca
# padrao do Python. As dependencias abaixo sao para a interface web e o PDF.
# (pywebview foi removido: dependia de componentes nativos do Windows que
# nao consigo testar de verdade neste ambiente e era o principal suspeito
# de causar falha silenciosa ao abrir o .exe - o navegador padrao e mais
# simples e mais confiavel.)

Flask==3.1.3
reportlab==4.4.10
pyinstaller==6.11.1
